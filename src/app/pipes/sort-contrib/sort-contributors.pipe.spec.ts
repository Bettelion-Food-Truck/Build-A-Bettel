import { SortContributorsPipe } from './sort-contributors.pipe';

describe('SortContributorsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortContributorsPipe();
    expect(pipe).toBeTruthy();
  });
});
