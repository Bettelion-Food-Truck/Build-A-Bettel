import { SortContributorImagesPipe } from './sort-contributors-images.pipe';

describe('SortContributorImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortContributorImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
